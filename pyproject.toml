[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rdc-tool"
version = "1.0.0"
description = "Local-first RenderDoc CLI runtime for .rdc replay and agent hosts."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "Apache-2.0" }
authors = [{ name = "CGBull" }]
dependencies = [
  "aiofiles>=23.0",
  "jinja2>=3.1",
  "numpy>=1.24",
  "Pillow>=10.0",
  "pydantic>=2.0",
]

[project.scripts]
rdc-tool = "rdc_tool.cli:main"

[project.optional-dependencies]
dev = [
  "pytest>=9.0",
]

[tool.setuptools]
package-dir = { "" = "." }

[tool.setuptools.packages.find]
where = ["."]
include = ["rdc_tool*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [".git", "intermediate", "binaries"]
markers = [
  "unit: fast behavior/unit checks that do not require a capture sample",
  "contract: contract and schema checks against catalog or transport adapters",
  "fixture_integration: checks that require an explicit external capture sample",
  "gpu_live: checks that require a live RenderDoc/GPU environment",
]
