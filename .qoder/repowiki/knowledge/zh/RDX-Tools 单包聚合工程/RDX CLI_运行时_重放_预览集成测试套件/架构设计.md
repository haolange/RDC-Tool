测试以 `conftest.py` 为根配置：通过 `autouse=True` 的 `_isolate_runtime_state` fixture 在每个用例前后清理 `runtime_state*.json`、`runtime_logs*.jsonl`、`context_snapshot*.json` 并调用 `clear_context_state` / `clear_context_snapshot`，保证用例间状态隔离；同时注入 `RDC_TOOL_ROOT`、`RDC_TOOL_ARTIFACT_DIR`、`RDC_TOOL_RENDERDOC_PATH`、`RDC_TOOL_RUNTIME_DLL_DIR`、`RDC_TOOL_INTERMEDIATE_ROOT` 等环境变量，使被测代码直接指向仓库源码目录下的二进制与 Python 模块。测试按功能域组织在独立文件中（CLI 参数解析、daemon/client、runtime worker、replay、preview、VFS、layout、性能服务失败处理、脚本治理等），并通过 `monkeypatch` 替换 `subprocess.Popen`、`rdc_tool.server`、`rdc_cli._daemon_exec` 等外部依赖来模拟真实环境。`fixtures/` 提供固定大小的 `.rdc` 捕获文件，由 `test_docs_and_fixtures.py` 校验其 SHA256 与大小，确保回放测试数据稳定可复现。