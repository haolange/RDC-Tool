工作进程通过环境变量 RDX_CONTEXT_ID、RDX_DAEMON_PID、RDX_RUNTIME_DLL_DIR、RDX_RENDERDOC_PATH、RDX_WORKER_SOURCE_MANIFEST 注入运行环境；CLI 入口应使用 rdx --json doctor 或 rdx call <rd.*>，直接调用 rdx.server.main() 会抛出 RuntimeError 提示正确用法。