- 所有面向用户的文档以 CLI 命令和标准 JSON 行为为描述主体，避免暴露 Python 启动内部细节。
- 生成的产物（`spec/tool_catalog.json`、`docs/tool-reference.md`）仅由代码驱动重新生成，禁止手动补丁或叠加元数据。
- 涉及 `rd.session.open_preview` 或预览几何的变更需同步更新 `preview_geometry_smoke.py` 与相关用户文档。
- 公共契约、稳定性策略与接口升级说明分离在独立文件中，便于单独审阅与维护。