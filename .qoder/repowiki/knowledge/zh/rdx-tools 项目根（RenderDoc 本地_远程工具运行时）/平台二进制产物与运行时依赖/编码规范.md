- 平台产物按 `binaries/<platform>/<arch>/` 目录结构隔离，新增平台需在顶层新增同构子目录。
- Vulkan 层能力通过 `renderdoc.json` 声明式配置（名称、类型、library_path、api_version、instance/device_extensions、环境变量开关），而非硬编码。
- 所有分发文件均纳入 `manifest.runtime.json` 清单，以相对 path + size + sha256 三元组记录，确保构建产物不可变且可验证。