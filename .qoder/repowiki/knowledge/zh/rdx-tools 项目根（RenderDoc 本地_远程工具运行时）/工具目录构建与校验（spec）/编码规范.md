- 每个工具条目统一包含 name/group/description/parameter_raw/returns_raw/param_names/prerequisites/namespace/input_schema/scope/effects/evidence_kind/path_inputs 等固定字段，便于下游消费。
- prerequisites 使用 requires+via_tools+(可选 when) 三元组声明前置依赖，requires 限定在白名单集合内，when 仅允许空字符串或 options.remote_id_present。
- input_schema 采用 JSON Schema 子集（type/properties/required/additionalProperties/not/enum/minimum 等）精确描述参数契约，禁止 additionalProperties。
- 校验脚本通过分离的 _iter_readability_errors 与 _iter_schema_errors 函数按关注点拆分检查逻辑，并以递增退出码区分失败类别。
- 生成脚本以 __main__ 入口 + argparse 暴露 --out/--check 参数，并通过 sys.path.insert(0, ROOT) 将仓库根加入导入路径以访问 rdx.runtime_catalog。