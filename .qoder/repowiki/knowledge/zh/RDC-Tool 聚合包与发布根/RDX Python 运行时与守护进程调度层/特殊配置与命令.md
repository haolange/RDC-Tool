工作进程通过环境变量 RDC_TOOL_CONTEXT_ID、RDC_TOOL_DAEMON_PID、RDC_TOOL_RUNTIME_DLL_DIR、RDC_TOOL_RENDERDOC_PATH、RDC_TOOL_WORKER_SOURCE_MANIFEST 注入运行环境；CLI 入口应使用 `rdc-tool --json doctor` 或 `rdc-tool call <rd.*>`，直接调用 rdc_tool.server.main() 会抛出 RuntimeError 提示正确用法。