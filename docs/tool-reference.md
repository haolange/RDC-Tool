# Tool Reference

This file is generated from the structured definitions in `rdc_tool/operation_definitions.py`. Do not edit it by hand; run `python -B scripts/generate_tool_reference.py`.

- Tool count: 128
- Group count: 18
- Catalog fingerprint: `4eefd77d649bef8a03d53ab408f097314c781c33ff01b0eec436c5c1562caf4f`
- Canonical transport: `rdc-tool call <rd.*> --format json`
- Discovery: `rdc-tool tools list`, `rdc-tool tools search <query>`, and `rdc-tool tools describe <rd.*>`

The catalog declares what an operation accepts and what it can affect. Authorization is still enforced by the embedding host; a catalog entry does not grant access.

## Groups

| Group | Tools |
| --- | ---: |
| 3.7，缓冲区与网格数据访问 (Buffer & Mesh Data Access) | 6 |
| 3.2，捕获文件与 Replay Session (Capture & Replay) | 12 |
| 3.1，核心与环境管理 (Core & Environment) | 13 |
| 3.9，Shader Debugger 与像素/顶点级调试 (Shader Debugging) | 11 |
| 3.14，诊断工具 (Diagnostics, Heuristic Checks) | 1 |
| 3.3，事件导航与 API 检查 (Event Navigation & API Inspection) | 9 |
| 3.11，导出、报告与可复现打包 (Export & Reporting) | 6 |
| 3.15，专家级宏工作流 (Expert Macro Workflows) | 1 |
| 3.10，性能与高级分析 (Performance & Advanced Analysis) | 4 |
| 3.4，管线状态查询 (Pipeline State Inspection) | 7 |
| 3.12，远程连接与自动化抓帧 (Remote Capture & Target Control) | 11 |
| 3.5，资源探查 (Resource Inspection & History) | 6 |
| 3.17，上下文快照工具 (Context Snapshot Tools) | 12 |
| 3.8，Shader 探查、反射与替换 (Shader Inspection & Replacement) | 13 |
| 3.6，纹理数据访问与分析 (Texture Data Access & Analysis) | 8 |
| 3.16，通用辅助工具 (Utilities, Optional) | 3 |
| 3.18，VFS 导航工具 (VFS Navigation Tools) | 4 |
| 3.10，性能计数器与测量 (Performance Counters) | 1 |

## 3.7，缓冲区与网格数据访问 (Buffer & Mesh Data Access)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.buffer.get_data` | 读取 buffer 的原始字节数据（默认保存到文件以避免 JSON 过大）。 | `session_id`: string (required)<br>`buffer_id`: string (required)<br>`offset`: integer (optional; min=0)<br>`size`: integer (optional; min=0)<br>`output_path`: string (optional)<br>`as_base64`: boolean (optional)<br>`state`: string (optional; enum=current,capture_initial; default="current")<br>`event_id`: integer (optional; min=1) | data: resource identity, byte_size, state, resolved_event_id, restored_event_id, replay_state_restored=true, initial_provenance; base64 when requested (1 MiB limit, no automatic artifact), otherwise explicit output/artifact. Initial provenance unavailable, failed/incomplete read and restoration failure are distinct errors. Restoration failure requires closing and reopening the session. | scope=replay; effects=replay_position_temporary, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.buffer.get_structured_data` | 按结构化布局解码 buffer 内容（适用于 StructuredBuffer/SSBO/顶点数据等）。 | `session_id`: string (required)<br>`buffer_id`: string (required)<br>`layout`: object (required)<br>`offset`: integer (optional; min=0)<br>`count`: integer (optional)<br>`max_elements`: integer (optional)<br>`size`: integer (optional; min=0) | ok (bool)<br>data (dict): {elements}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.buffer.search_pattern` | 在 buffer 内搜索字节模式（适用于查找 magic number、NaN pattern、特定 id）。 | `session_id`: string (required)<br>`buffer_id`: string (required)<br>`pattern`: string (required)<br>`offset`: integer (optional; min=0)<br>`size`: integer (optional; min=0)<br>`max_results`: integer (optional) | ok (bool)<br>data (dict): {matches}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.mesh.decode_index_data` | 解码 Index Buffer（16/32-bit）为 index 列表。 | `session_id`: string (required)<br>`index_buffer_id`: string (required)<br>`format`: string (required; enum=u16,u32)<br>`index_offset`: integer (optional)<br>`index_count`: integer (optional)<br>`base_vertex`: integer (optional) | ok (bool)<br>data (dict): {indices}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.mesh.get_drawcall_mesh_config` | 获取当前 drawcall 的 mesh 配置：使用哪些 VB/IB、topology、baseVertex、indexOffset、instanceCount 等。 | `instance`: integer (optional; default=0; min=0)<br>`max_vertices`: integer (optional; default=128; min=0)<br>`session_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {mesh_config: {event_id, topology, bindings, vertex_input}}; vertex_input includes source=vs_input, vertex_rows with draw/vertex indices and typed semantic values, attributes position/normal/uv status, layout, index_binding, truncated. Values are shader inputs in original coordinate space; packed normal encoding is not interpreted as a geometric normal<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.mesh.get_post_transform_data` | Read post-transform vertex data from VS or GS at a replay event. view_index selects an integer multiview/VR view; unbound GS is reported explicitly. | `session_id`: string (required)<br>`event_id`: integer (optional; min=0)<br>`instance`: integer (optional; min=0)<br>`view_index`: integer (optional; default=0; min=0)<br>`max_vertices`: integer (optional; min=0)<br>`stage`: string (required; enum=vs,gs) | vertex_rows retain raw bytes and nullable typed position. attributes explicitly reports position/normal/uv present or unsupported; outputs separately exposes a verified D3D11/12 stream-zero float output signature layout and per-row outputs, without assigning TEXCOORD to normal. source=post_transform; no input values are substituted. No attribute layout is guessed. truncated is null when an unbounded native buffer view prevents proving total size.<br>ok (bool)<br>data (dict): {mesh_data}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.2，捕获文件与 Replay Session (Capture & Replay)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.capture.close_file` | 关闭捕获文件句柄。 | `capture_file_id`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=lifecycle; evidence=none | capture_file_id; via rd.capture.open_file |
| `rd.capture.close_replay` | 关闭一个 Replay Session，释放回放 driver 与缓存资源。 | `session_id`: string (optional) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=lifecycle; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.capture.get_info` | 获取捕获文件元数据（时间、可执行文件、驱动、机器信息、RDC 版本等）。 | `capture_file_id`: string (required) | ok (bool)<br>data (dict): {metadata}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=lifecycle; evidence=none | capture_file_id; via rd.capture.open_file |
| `rd.capture.open_file` | 打开一个 .rdc 捕获文件并返回文件句柄（不自动创建 Replay）。 | `file_path`: string (required)<br>`read_only`: boolean (optional) | ok (bool)<br>data (dict): {capture_file_id, driver}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=lifecycle; evidence=none | - |
| `rd.capture.open_replay` | 基于 capture_file_id 创建一个可回放的 Replay Session；若提供 options.remote_id，则必须严格走该 live remote handle 对应的 remote backend，不允许 silent local fallback。 | `capture_file_id`: string (required)<br>`options`: object (optional) | ok (bool)<br>data (dict): {session_id, capture_file_id, frame_count, active_event_id, api_properties, recovery_status, reused_session?}<br>artifacts (list)<br>error (dict\|null): remote handle errors remain explicit; same-context live sessions for the same capture are reused with reused_session=true. Stale same-capture sessions are cleaned before reopen; cleanup failure returns stale_session_requires_restart with recovery steps.<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=lifecycle; evidence=none | capture_file_id; via rd.capture.open_file<br>remote_id; via rd.remote.connect; when options.remote_id_present |
| `rd.event.get_active` | 读取当前 active event。 | `session_id`: string (required) | ok (bool)<br>data (dict): {active_event_id}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.set_active` | 设置当前 active event；成功后 runtime 与 context snapshot 必须同步到同一个 resolved event，失败时保持旧状态不变。 | `session_id`: string (required)<br>`event_id`: integer (required; min=0) | ok (bool)<br>data (dict): {active_event_id}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.replay.get_api_properties` | 获取 Replay Session 的 API 属性（如 shader debug 支持、counter 支持等）。 | `session_id`: string (required) | ok (bool)<br>data (dict): {api_properties}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.replay.get_driver_info` | 获取回放 driver 信息（版本、vendor、device、driver 字符串等）。 | `session_id`: string (required) | ok (bool)<br>data (dict): {driver_info}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.replay.get_frame_info` | 获取当前/指定帧的统计信息（drawcall 数、marker 数、时长等）。 | `session_id`: string (required)<br>`frame_index`: integer (optional) | ok (bool)<br>data (dict): {frame_info}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.replay.set_frame` | 切换当前回放帧（会重置 active event 到该帧起始）。 | `session_id`: string (required)<br>`frame_index`: integer (required) | ok (bool)<br>data (dict): {active_event_id}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.capture.get_thumbnail` | Read embedded capture thumbnail without replay. | `capture_file_id`: string (required)<br>`format`: string (optional; enum=png,jpg; default="png")<br>`max_size`: integer (optional; default=256; min=1; max=2048)<br>`output_path`: string (optional) | data: capture_file_id, source=embedded_capture_thumbnail, format, byte_size, base64 or saved_path. Distinguishes invalid capture, missing thumbnail and read failure. | scope=capture; effects=artifact_write; evidence=none | capture_file_id; via rd.capture.open_file |

## 3.1，核心与环境管理 (Core & Environment)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.core.get_capabilities` | 获取当前环境能力详情；除 replay/remote/debug 基础项外，还会暴露当前 context/session/backend 视角下的 remote capability matrix。 | `detail_level`: string (optional) | ok (bool)<br>data (dict): {capabilities}; `capabilities` 至少包含 replay / remote / shader_debug / shader_replace / shader_compile / counters / artifact_dir / context_id / current_session_id / current_backend，并在 remote 相关场景下附带 `remote_capability_matrix`<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_config` | 读取当前运行时配置。 | - | ok (bool)<br>data (dict): {config}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_logs` | 读取内部日志（可按时间/级别过滤）。 | `since_ms`: integer (optional)<br>`level_min`: string (optional)<br>`max_lines`: integer (optional) | ok (bool)<br>data (dict): {logs}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_operation_history` | 读取当前 context 最近的 trace-linked 操作历史，可按时间、状态与操作名过滤。 | `since_ms`: integer (optional)<br>`operation`: string (optional)<br>`status`: string (optional)<br>`max_items`: integer (optional) | ok (bool)<br>data (dict): {context_id, operations}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_runtime_metrics` | 读取当前 context 的自监控指标、限制配置、恢复摘要与最近操作统计。 | - | ok (bool)<br>data (dict): {context_id, limits, metrics, recovery, recent_operations}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_tool_graph` | 返回 tool 之间的 prerequisite 与 macro-to-canonical 依赖图，图中 nodes 沿用 discovery summary 与默认排序，帮助 Agent 先看 canonical 主接口，再看 macro 和 navigation 辅助层。 | `query`: string (optional)<br>`namespace`: string (optional)<br>`capability`: string (optional)<br>`role`: string (optional)<br>`intent`: string (optional) | ok (bool)<br>data (dict): {tools: discovery summaries with name, namespace, group, description, role, discovery_rank, capabilities, intents, prerequisites, supports_projection?, recommended_for?, not_primary_for?, edges}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.get_version` | 获取 RenderDoc 运行时版本与构建信息。 | - | ok (bool)<br>data (dict): {version, commit_hash, build_date}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.healthcheck` | 执行自检：验证 RenderDoc 库加载、临时目录写入、（可选）创建最小 Replay/Remote 连接等。 | `check_remote`: boolean (optional)<br>`check_replay`: boolean (optional) | ok (bool)<br>data (dict): {checks}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.init` | 初始化 RenderDoc Replay/Remote 运行时；建立对象池与临时目录；必须在首次使用前调用一次。 | `global_env`: object (optional)<br>`enable_remote`: boolean (optional) | ok (bool)<br>data (dict): {api_version, capabilities}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=global_config; evidence=none | - |
| `rd.core.list_tools` | 按 namespace、group、capability、role、intent 等结构化条件列出可用 tool，默认仍以 canonical `rd.*` 主接口优先，其后才是 macro、session/context/core 元信息层与 navigation 辅助层。 | `namespace`: string (optional)<br>`group`: string (optional)<br>`capability`: string (optional)<br>`role`: string (optional)<br>`intent`: string (optional)<br>`mutates_state`: boolean (optional)<br>`detail_level`: string (optional)<br>`query`: string (optional) | ok (bool)<br>data (dict): {tool_count, tools: discovery summaries with name, namespace, group, description, role, discovery_rank, capabilities, intents, prerequisites, supports_projection?, recommended_for?, not_primary_for?}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | - |
| `rd.core.set_config` | 设置运行时配置（对后续调用生效）。 | `config`: object (required) | ok (bool)<br>data (dict): {applied}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=global_config; evidence=none | - |
| `rd.core.set_log_level` | 设置内部日志等级，便于定位 CLI runtime 与 RenderDoc 侧错误。 | `level`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=global_config; evidence=none | - |
| `rd.core.shutdown` | 关闭运行时并释放所有资源（Replay Session、Remote 连接、临时文件句柄）。 | - | ok (bool)<br>data (dict): {released}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=global_config; evidence=none | - |

## 3.9，Shader Debugger 与像素/顶点级调试 (Shader Debugging)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.debug.clear_breakpoints` | 清除所有断点。 | `session_id`: string (required)<br>`shader_debug_id`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.continue` | 继续执行 debugger（直到断点/结束/超时）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`timeout_ms`: integer (optional) | ok (bool)<br>data (dict): {state, stopped_reason}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.evaluate_expression` | 在 debugger 上下文中求值表达式（如 'cb0.myVar' 或寄存器表达式；实现可选）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`expression`: string (required) | ok (bool)<br>data (dict): {value}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.finish` | 结束并释放 shader debug session。 | `session_id`: string (required)<br>`shader_debug_id`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.get_callstack` | 获取 shader 调用栈（函数名/文件/行）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required) | ok (bool)<br>data (dict): {callstack}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.get_variables` | 获取当前 scope 的变量（支持按名称过滤/按路径展开结构体）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`name_filter`: string (optional)<br>`expand_depth`: integer (optional)<br>`max_variables`: integer (optional) | ok (bool)<br>data (dict): {variables}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.run_to` | 运行到指定位置（PC 或 source location）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`target`: object (required)<br>`timeout_ms`: integer (optional) | ok (bool)<br>data (dict): {state}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.set_breakpoints` | 设置断点列表（按 PC 或 source location）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`breakpoints`: array[any] (required) | ok (bool)<br>data (dict): {active_breakpoints}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.debug.step` | 单步执行 debugger（步进一条指令或源代码行）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`step_mode`: string (optional) | ok (bool)<br>data (dict): {state}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.debug_start` | 启动 shader debugger（默认 pixel），在 remote replay 下会先读取 remote capability matrix；如果当前 backend/session 明确不支持 debug，则在创建 trace 前直接 truthful-fail。 | `session_id`: string (required)<br>`mode`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`params`: object (required)<br>`timeout_ms`: integer (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {shader_debug_id, initial_state, resolved_context, resolved_event_id, pixel_history_summary, selected_target_source, failure_stage, failure_reason, attempts}<br>artifacts (list)<br>error (dict\|null): remote backend/session 明确不支持时，优先返回 capability-style 错误，而不是等到 trace 创建后才报 `debugger_handle_missing`<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_debug, replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_debug_state` | 获取当前 debugger 状态（当前 PC、活跃函数、变量、寄存器等）。 | `session_id`: string (required)<br>`shader_debug_id`: string (required)<br>`detail_level`: string (optional)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {state}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.14，诊断工具 (Diagnostics, Heuristic Checks)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.diag.scan_common_issues` | 对当前 active event 执行一组常见问题扫描（空绑定、viewport/scissor、depth/blend、格式不匹配等），输出结构化 issue 列表。 | `session_id`: string (required)<br>`severity_min`: string (optional)<br>`include_suggestions`: boolean (optional) | ok (bool)<br>data (dict): {issues}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.3，事件导航与 API 检查 (Event Navigation & API Inspection)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.event.diff_pipeline_state` | 对比两个 Event 的 Pipeline State 差异，可用于定位“状态被谁修改”的问题。 | `session_id`: string (required)<br>`event_a`: integer (required)<br>`event_b`: integer (required)<br>`scope`: string (optional)<br>`include_unchanged`: boolean (optional) | ok (bool)<br>data (dict): {diff}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_action_details` | 获取单个 Action 的详细信息，包括 Drawcall 参数、Marker 范围以及输出目标等摘要。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0) | Returns parent_chain, nullable marker_path and depth_output; ResourceId is local to this capture.<br>ok (bool)<br>data (dict): {action}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_action_tree` | 获取当前帧完整的 Action 树，支持按深度或过滤条件进行裁剪。 | `session_id`: string (required)<br>`max_depth`: integer (optional)<br>`filter`: object (optional)<br>`offset`: integer (optional; min=0)<br>`limit`: integer (optional; min=1)<br>`max_nodes`: integer (optional; min=1)<br>`event_id`: integer (optional; min=0)<br>`include_markers`: boolean (optional; default=true)<br>`include_drawcalls`: boolean (optional; default=true)<br>`projection`: object (optional) | ok (bool)<br>data (dict): {root, pagination}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_callstack` | 获取指定 Event 的 CPU Callstack（如果捕获时包含该信息且平台支持）。 | `session_id`: string (required)<br>`event_id`: integer (required; min=0)<br>`resolve_symbols`: boolean (optional) | ok (bool)<br>data (dict): {callstack}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_parent_chain` | 获取指定 Event 的父链（从根节点到当前 Event），用于还原 Marker Stack 或 Pass 层级。 | `session_id`: string (required)<br>`event_id`: integer (required; min=0) | ok (bool)<br>data (dict): {parent_chain}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_resource_usage` | 获取指定 Event 上资源的摘要使用情况，例如哪些资源被绑定为 RT/DS/SRV/UAV/CBV/IB/VB 等。 | `session_id`: string (required)<br>`event_id`: integer (required; min=0) | ok (bool)<br>data (dict): {usage}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.list_passes` | 根据 Marker 或 Region 归纳当前帧的 Pass 列表，类似于 RenderDoc Event Browser 的 Region 视图。 | `session_id`: string (required)<br>`marker_policy`: string (optional) | ok (bool)<br>data (dict): {passes}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.search_actions` | 在当前帧的 Action 树中搜索节点，支持按名称、Flag 或事件范围等条件。 | `session_id`: string (required)<br>`query`: object (required)<br>`max_results`: integer (optional) | ok (bool)<br>data (dict): {matches}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.event.get_api_calls` | Read captured calls associated with an action or explicit chunk indices. | `session_id`: string (required)<br>`event_id`: integer (optional; min=1)<br>`chunk_indices`: array[integer] (optional)<br>`offset`: integer (optional; default=0; min=0; max=2147483647)<br>`limit`: integer (optional; default=50; min=1; max=200)<br>`max_nodes`: integer (optional; default=4096; min=1; max=16384)<br>`object_path`: array[integer] (optional)<br>`child_offset`: integer (optional; default=0; min=0)<br>`value_offset`: integer (optional; default=0; min=0) | data: typed calls, total, offset, next_offset, node_budget_exhausted. Exactly one event_id/chunk_indices; child and character omissions explicit. Buffers referenced without automatic export. Invalid event/chunk and missing structured data fail. Each node includes object_path; continue a single chunk using next_child_offset or next_value_offset. | scope=replay; effects=none; evidence=none | session_id; via rd.capture.open_replay |

## 3.11，导出、报告与可复现打包 (Export & Reporting)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.export.buffer` | 将 buffer 内容导出到文件；这是统一的 buffer 落盘导出入口。 | `session_id`: string (required)<br>`buffer_id`: string (required)<br>`output_path`: string (required)<br>`offset`: integer (optional; min=0)<br>`size`: integer (optional; min=0) | ok (bool)<br>data (dict): {saved_path}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.export.cbuffer_dump` | 导出当前 event 的常量缓冲数据（按 stage/slot 命名），支持结构化变量值。 | `session_id`: string (required)<br>`output_dir`: string (required)<br>`stages`: array[string] (optional)<br>`slots`: array[any] (optional)<br>`max_bytes`: integer (optional)<br>`include_raw`: boolean (optional)<br>`include_decoded`: boolean (optional) | ok (bool)<br>data (dict): {dumped_paths, saved_files, failures, partial, stages, slots}; each stage/slot is handled independently and partial failures are recorded instead of blocking the whole request.<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.export.mesh` | Export post-VS positions or explicit VS input position/normal/UV as Wavefront OBJ. Supports float32 positions and triangle list/strip, line list or point list topology; VS input attributes require float semantics in original input space; postvs remains positions only. | `session_id`: string (required)<br>`event_id`: integer (optional; min=0)<br>`format`: string (optional; enum=obj; default="obj")<br>`output_path`: string (required)<br>`include_attributes`: boolean (optional; default=false)<br>`space`: string (optional; enum=postvs,vs_input; default="postvs") | JSON declares exported attributes. VS input with include_attributes=true exports v/vt/vn with matching face indices; postvs is positions only.<br>data: {saved_path,export_format,space,include_attributes,resolved_event_id,vertex_count,primitive_count}. Unsupported geometry produces mesh_export_unsupported; no placeholder file is written. | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.export.screenshot` | 导出当前 event 的输出截图（等价于 UI 的保存 framebuffer）。 | `session_id`: string (required)<br>`target`: object (optional)<br>`event_id`: integer (optional; min=0)<br>`output_path`: string (required)<br>`file_format`: string (optional)<br>`include_alpha`: boolean (optional)<br>`overlay`: string (optional) | ok (bool)<br>data (dict): single export returns {artifact_path, saved_path, image_path, meta, resolved_event_id, selected_formats, requested_formats, recommended_formats, name_info, texture_format, chosen_output_slot, texture_id, target_source, binding_truth_level, visual_truth_level, evidence_truth_level, summary_degraded_reasons}; multi-format export returns {exports, saved_paths, image_paths, resolved_event_id, selected_formats, requested_formats, recommended_formats, name_info, texture_format, chosen_output_slot, texture_id, target_source, binding_truth_level, visual_truth_level, evidence_truth_level, summary_degraded_reasons}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=measurement | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.export.shader_bundle` | 导出当前 event 相关 shader 的 bundle；event-bound shader 绑定与 `rd.pipeline.get_shader` / `rd.shader.get_disassembly` / `rd.shader.get_reflection` / `rd.shader.edit_and_replace` 保持一致。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0)<br>`output_dir`: string (required)<br>`include_binaries`: boolean (optional)<br>`include_reflection`: boolean (optional)<br>`include_disassembly`: boolean (optional) | ok (bool)<br>data (dict): {output_dir, bundle_path, resolved_event_id}<br>artifacts (list)<br>error (dict\|null): 当请求 event 没有可绑定 shader 时，显式返回 `shader_bundle_empty`<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.export.texture` | 将纹理导出为可直接打开的图片/导出文件；这是统一的纹理导出入口，支持子资源选择、显示映射与通道控制。 | `event_id`: integer (optional; min=0)<br>`session_id`: string (required)<br>`texture_id`: string (required)<br>`subresource`: object (optional)<br>`output_path`: string (required)<br>`file_format`: string (optional)<br>`channels`: string (optional)<br>`remap`: object (optional)<br>`flip_y`: boolean (optional) | ok (bool)<br>data (dict): {artifact_path, saved_path, meta, selected_formats, requested_formats, recommended_formats, name_info, texture_format}; multi-format export returns {exports, saved_paths, selected_formats, requested_formats, recommended_formats}. Explicit HDR/EXR/DDS requests never silently downgrade to PNG; unavailable encoders return ok=false with format_encoder_unavailable/format_not_supported, requested_formats, supported_formats, recommended_formats, actual_format, and downgrade_allowed=false.<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=measurement | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.15，专家级宏工作流 (Expert Macro Workflows)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.macro.find_state_change_point` | 在 event 区间内搜索“某个状态何时从 A 变为 B”（通过 pipeline diff 快速二分/线性扫描）。 | `session_id`: string (required)<br>`event_range`: object (required)<br>`state_path`: string (required)<br>`target_value`: any (required)<br>`max_events`: integer (optional; default=2000; min=1) | data: {found_event_id: integer\|null, examined_events: integer, complete: boolean, range_exhausted: boolean, budget_exhausted: boolean}. Visits real event IDs only; cancellation interrupts between events. | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.10，性能与高级分析 (Performance & Advanced Analysis)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.perf.describe_counter` | 获取单个 counter 的详细描述。 | `session_id`: string (required)<br>`counter_id`: integer (required) | ok (bool)<br>data (dict): {counter}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=none; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.perf.enumerate_counters` | 枚举可用的 GPU counters。 | `session_id`: string (required) | ok (bool)<br>data (dict): {counters}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=none; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.perf.get_event_durations` | 获取每个 event 的 GPU duration（若支持 timing queries；可用于定位慢 draw）。 | `session_id`: string (required)<br>`event_range`: object (optional)<br>`max_events`: integer (optional) | ok (bool)<br>data (dict): {durations}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=measurement | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.perf.sample_counters` | 对当前帧/指定 event 范围采样 counters（可能较慢）。 | `session_id`: string (required)<br>`counter_ids`: array[any] (required)<br>`event_range`: object (optional)<br>`stride`: integer (optional) | ok (bool)<br>data (dict): {samples}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=measurement | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.4，管线状态查询 (Pipeline State Inspection)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.pipeline.get_constant_buffers` | 获取指定 stage 的 Constant Buffer 绑定（slot/buffer/offset/size）。 | `session_id`: string (required)<br>`stage`: string (required)<br>`include_contents`: boolean (optional)<br>`max_bytes`: integer (optional)<br>`event_id`: integer (optional; min=0)<br>`array_index`: integer (optional; min=0)<br>`flatten`: boolean (optional)<br>`slot`: integer (optional; min=0) | ok (bool)<br>data (dict): {constant_buffers}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_index_buffer` | 获取当前绑定的 Index Buffer（resource_id/format/offset）。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {index_buffer}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_resource_bindings` | 获取指定 stage 的资源绑定（SRV/CBV/RO-Images 等）。 | `session_id`: string (required)<br>`stage`: string (required)<br>`type`: string (optional; enum=SRV,UAV,CBV,Sampler)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {bindings}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_shader` | 获取指定 stage 的 shader 句柄与摘要信息（resource_id、entry、debug info 可用性等）。 | `session_id`: string (required)<br>`stage`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {shader}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_stage_state` | 获取指定 Shader Stage 的 Pipeline 子状态。 | `session_id`: string (required)<br>`stage`: string (required)<br>`event_id`: integer (optional; min=0)<br>`max_bytes`: integer (optional; min=0) | ok (bool)<br>data (dict): {stage_state}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_state` | 获取当前 active event 的完整 Pipeline State（返回结构按 API 类型组织）。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0)<br>`context_id`: string (optional)<br>`detail`: string (optional; enum=summary,full; default="full")<br>`sections`: array[string] (optional) | Shader hash is SHA-256 of recorded shader rawBytes, or null when unavailable; debug_name is the native non-autogenerated shader resource debug name or null; resource_name retains the native display name including autogenerated names; debug_source_files lists actual debug source filenames separately. Neither is derived from ResourceId.<br>data: resolved_event_id, pipeline_state. Full indexed viewports/scissors, blend options and complete front/back stencil. API-specific sections carry api, status and native data; failed reads are not empty states. | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.pipeline.get_vertex_buffers` | 获取当前绑定的 Vertex Buffers 列表（含 slot/stride/offset/resource_id）。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {vertex_buffers}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.12，远程连接与自动化抓帧 (Remote Capture & Target Control)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.remote.connect` | 连接到远程 RenderDoc server；支持 direct RenderDoc endpoint 和 Android adb bootstrap。 | `host`: string (optional)<br>`port`: integer (optional)<br>`timeout_ms`: integer (optional)<br>`options`: object (optional) | ok (bool)<br>data (dict): {remote_id, server_info}; for adb_android, server_info/bootstrap details identify the endpoint, device and ownership: started_activity/owned_pids describe only this attempt; existing helpers are borrowed without install, configuration or launch. Success requires RenderDoc connection and Ping. Socket readiness waits at most 15 seconds within the total timeout; query failure, missing/ambiguous socket and native busy/incompatible status remain distinct. Disconnect closes only this connection and verified owned resources. Cleanup failures accompany the original error.<br>artifacts (list)<br>error (dict\|null): direct renderdoc transport requires host; adb_android may omit host and defaults to the local bootstrap endpoint; unsupported transports return `remote_transport_unsupported`<br>meta (dict)<br>projections (dict, optional) | scope=global; effects=remote_control; evidence=none | capability.remote; via rd.core.init |
| `rd.remote.copy_capture` | 将远程 capture 拷贝到本地路径（也可用于下载）。 | `remote_id`: string (required)<br>`capture_id`: integer (required)<br>`local_path`: string (required) | ok (bool)<br>data (dict): {saved_path}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.delete_capture` | 删除远程服务器上的 capture（谨慎使用）。 | `remote_id`: string (required)<br>`capture_id`: integer (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.disconnect` | 断开远程连接。 | `remote_id`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.launch_app` | 在远程机器启动应用并注入 RenderDoc（若 server 支持）。 | `remote_id`: string (required)<br>`exe_path`: string (required)<br>`working_dir`: string (optional)<br>`cmdline`: string (optional)<br>`env`: object (optional)<br>`capture_options`: object (optional) | ok (bool)<br>data (dict): {target_id, pid}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.list_captures` | 列出远程服务器上所有可用捕获。 | `remote_id`: string (required) | ok (bool)<br>data (dict): {captures}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.list_targets` | 列出远程可连接的 targets（已注入/已运行应用、可抓帧进程等）。 | `remote_id`: string (required) | ok (bool)<br>data (dict): {targets}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.ping` | 检查远程连接可用性与延迟。 | `remote_id`: string (required) | ok (bool)<br>data (dict): {latency_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.queue_capture` | 在远程 target 上排队捕获（例如下一次 Present/下一帧），并返回队列状态。 | `remote_id`: string (required)<br>`target_id`: string (optional)<br>`num_frames`: integer (optional) | ok (bool)<br>data (dict): {queue_status}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.set_capture_options` | 设置远程抓帧参数（保存路径、frame limit、禁止/允许某些 API 特性等）。 | `remote_id`: string (required)<br>`options`: object (required) | ok (bool)<br>data (dict): {applied}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |
| `rd.remote.trigger_capture` | 在远程 target 上触发一次或多次捕获。 | `remote_id`: string (required)<br>`target_id`: string (optional)<br>`num_frames`: integer (optional)<br>`capture_delay_ms`: integer (optional) | ok (bool)<br>data (dict): {captures}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=remote_control; evidence=none | remote_id; via rd.remote.connect<br>capability.remote; via rd.core.init |

## 3.5，资源探查 (Resource Inspection & History)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.resource.estimate_memory` | 估算资源占用内存，按格式/维度推算，用于快速筛选大资源。 | `session_id`: string (required)<br>`resource_id`: string (required) | ok (bool)<br>data (dict): {estimated_bytes, breakdown}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.resource.get_details` | 获取资源详细信息，兼容 V3；根据资源类型返回不同字段。 | `session_id`: string (required)<br>`resource_id`: string (required)<br>`include_initialization`: boolean (optional; default=false) | ok (bool)<br>data (dict): {details}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.resource.get_usage` | 获取资源的使用情况，即哪些 event 读/写/绑定该资源。 | `session_id`: string (required)<br>`resource_id`: string (required)<br>`max_events`: integer (optional) | Usage includes native usage_name, nullable is_read/is_write, binding_only_observable=false, event identity and color/depth attachments. These are recorded usages, not per-pixel access proof.<br>ok (bool)<br>data (dict): {usage}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.resource.list_all` | 列出捕获中所有资源（Textures/Buffers/Shaders/Pipelines 等），兼容 V3。 | `session_id`: string (required)<br>`types`: array[string] (optional)<br>`include_unused`: boolean (optional)<br>`kind`: string (optional; enum=all,texture,buffer,descriptor_store; default="all") | ok (bool)<br>data (dict): {resources}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.resource.set_alias` | 为资源设置一个本地别名，仅影响后续输出/报告，不修改捕获内容。 | `session_id`: string (required)<br>`resource_id`: string (required)<br>`alias`: string (required) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=context_metadata; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.resource.get_descriptors` | Read descriptor-store ranges including entries not accessed by the current action. | `session_id`: string (required)<br>`descriptor_store_id`: string (required)<br>`event_id`: integer (optional; min=1)<br>`first`: integer (optional; default=0; min=0; max=2147483647)<br>`count`: integer (optional; default=64; min=1; max=1024)<br>`type`: string (optional; enum=resource,sampler; default="resource") | data: descriptor_store_id, resolved_event_id, indexed descriptors and logical locations, total, next_first. Empty slots retained; invalid ranges and incomplete reads fail. | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_replay |

## 3.17，上下文快照工具 (Context Snapshot Tools)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.session.clear_context` | Clear an isolated CLI daemon context namespace and its runtime state. | `target_context_id`: string (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, backend, runtime, remote, focus, notes, current_session_id, sessions, recovery, limits, updated_at_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=lifecycle; evidence=none | - |
| `rd.session.close_preview` | Close the preview window for the selected context. | `context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, preview{enabled,state,view_mode,bound_session_id,bound_capture_file_id,bound_event_id,backend,recovered_from_session_id,rebind_count,last_error,display,updated_at_ms}}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=desktop_window; evidence=none | - |
| `rd.session.create_context` | Create or initialize an isolated CLI daemon context namespace. | `new_context_id`: string (required)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, backend, runtime, remote, focus, notes, last_artifacts, current_session_id, sessions, recovery, limits, updated_at_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=lifecycle; evidence=none | - |
| `rd.session.get_context` | 读取当前 context 快照与持久化状态索引，返回 runtime、remote、focus、session 表、恢复信息、最近操作与限制配置。 | `context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, backend, session_locator, runtime, remote, focus, notes, last_artifacts, preview{enabled,state,view_mode,bound_session_id,bound_capture_file_id,bound_event_id,backend,recovered_from_session_id,rebind_count,last_error,display{output_slot,texture_id,texture_format,framebuffer_extent,viewport_rect,scissor_rect,effective_region_rect,region_marker_mode,window_rect,fit_mode,screen_cap_ratio},updated_at_ms}, current_session_id, sessions, recovery, limits, active_operation, recent_operations, updated_at_ms, remote_capability_matrix, remote_context_locality, remote_handle_origin_context, remote_handle_reuse_policy}; remote_capability_matrix is state-first for get_context and may report not_currently_probed for live shader/debug/replace capability probes<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=none; evidence=none | - |
| `rd.session.get_replay_events` | Read the complete discrete action event index of the selected live context without changing its event. Events include parent_chain and nullable marker_path. An explicit session_id must belong to the same daemon context. | `session_id`: string (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {data, error}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=context; effects=none; evidence=none | session_id; via rd.capture.open_replay |
| `rd.session.list_contexts` | List known isolated CLI daemon context namespaces. | `context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, contexts: [{context_id, backend, session_locator, current_session_id, current_capture_file_id, session_count, capture_count, updated_at_ms}]}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=none; evidence=none | - |
| `rd.session.list_sessions` | 列出当前 context 下的持久化 session 表、当前选中 session 与恢复摘要。 | `context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, backend, current_session_id, sessions, recovery, limits}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=context; effects=none; evidence=none | - |
| `rd.session.observe` | Atomically apply an event and export its color output without a desktop window; missing event observes the current state. Remote device presentation is reported separately from PNG export, using a fresh native acknowledgement for the applied event and selected texture. No color target clears the native output. Missing native capability is unsupported; surface and presentation failures are unavailable. | `out_path`: string (required)<br>`event_id`: integer (optional; min=0)<br>`final_output`: boolean (optional)<br>`target`: object (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, session_id, revision, event_id, image_event_id, image_path, target, targets, image_error, remote_display{status: presented\|unavailable\|unsupported\|not_applicable, event_id, texture_id, sequence, reason}}. Presented requires successful native GPU presentation and a fresh positive sequence. No-color output is unavailable with a successful clear sequence when the surface was cleared; failures never reuse a prior receipt.<br>artifacts (list)<br>error (dict\|null)<br>meta (dict) | scope=context; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_replay |
| `rd.session.open_preview` | Open or rebind the preview window for the selected context session. | `session_id`: string (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, preview{enabled,state,view_mode,bound_session_id,bound_capture_file_id,bound_event_id,backend,recovered_from_session_id,rebind_count,last_error,display{output_slot,texture_id,texture_format,framebuffer_extent,viewport_rect,scissor_rect,effective_region_rect,region_marker_mode,window_rect,fit_mode,screen_cap_ratio},updated_at_ms}}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, optional) | scope=context; effects=desktop_window; evidence=none | - |
| `rd.session.resume` | 基于持久化状态尝试恢复当前 context 的本地 `.rdc` session；可选仅校验指定 session 的恢复结果。 | `session_id`: string (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, runtime, remote, focus, current_session_id, sessions, recovery, limits, recent_operations, updated_at_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=context; effects=lifecycle; evidence=none | - |
| `rd.session.select_session` | 切换当前 context 的 current session 指针，而不销毁其他已持有的 session。 | `session_id`: string (required)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, runtime, remote, focus, current_session_id, sessions, recovery, limits, recent_operations, updated_at_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=context; effects=lifecycle; evidence=none | - |
| `rd.session.update_context` | 更新当前 context 的 user-owned 字段，例如 focus_pixel、focus_resource_id、focus_shader_id 与 notes。 | `key`: string (required)<br>`value`: any (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {context_id, runtime, remote, focus, notes, last_artifacts, updated_at_ms}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=context; effects=context_metadata; evidence=none | - |

## 3.8，Shader 探查、反射与替换 (Shader Inspection & Replacement)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.shader.compile` | （可选扩展）在 server 侧调用外部编译器（dxc/glslangValidator/metal）编译 shader，返回 binary 路径；用于构建替换输入。 | `source_text`: string (optional)<br>`source_path`: string (optional)<br>`stage`: string (required)<br>`entry`: string (required)<br>`target`: string (required)<br>`source_encoding`: string (optional)<br>`defines`: object (optional)<br>`include_dirs`: array[string] (optional)<br>`additional_args`: array[string] (optional)<br>`output_path`: string (optional)<br>`session_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {session_id, shader_id, entry, target, stage, source_kind, resolved_source_path, include_dirs, source_encoding, supported_source_encodings, runtime_replacement_supported, runtime_replacement_reason, messages, compiler_messages, edit_plan, saved_path?, artifact_path?, byte_size?}<br>artifacts (list)<br>error (dict\|null): compile validation/build failures include source_kind, resolved_source_path, include_dirs, entry, target, and failure_stage. Unsupported source encoding returns shader_compile_encoding_unsupported. DXIL/DXBC captured disassembly text is not accepted as editable source.<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | - |
| `rd.shader.edit_and_replace` | 在当前 Replay Session 中替换 shader；支持 `event_id + stage` 与 `event_id + stage + shader_id` 两种入口，显式 `shader_id` 会优先作为绑定 identity 校验。 | `session_id`: string (required)<br>`stage`: string (required)<br>`event_id`: integer (optional; min=0)<br>`shader_id`: string (optional)<br>`ops`: array[any] (optional)<br>`source_text`: string (optional)<br>`source_path`: string (optional)<br>`entry`: string (optional)<br>`target`: string (optional)<br>`source_encoding`: string (optional)<br>`diff_text`: string (optional)<br>`emit_patch_artifacts`: boolean (optional)<br>`output_dir`: string (optional)<br>`expected_source_hash`: string (optional)<br>`include_dirs`: array[string] (optional)<br>`intent`: string (optional)<br>`max_diff_ops`: integer (optional; min=0)<br>`preserve_outputs`: boolean (optional)<br>`replacement_id`: string (optional)<br>`source_target`: string (optional) | ok (bool)<br>data (dict): {replacement_id, status='applied'\|'noop', resolved_event_id, messages, edit_plan, replacement{edit_plan, compile{encoding, disassembly_target, entry_point, compile_flags}, artifacts{source_before, source_after, patch_diff}}}<br>artifacts (list): when emit_patch_artifacts=true or output_dir is provided, includes before/after/diff text artifacts<br>error (dict\|null): DXIL/DXBC disassembly remains read-only (captured_source_editable=false), but user-provided HLSL/GLSL full replacement can compile then ReplaceResource when runtime_full_replace_supported=true. Build failures do not call ReplaceResource. Replace failures include replacement_attempted, cleanup_attempted, context_preserved, failure_stage, and failure_reason.<br>meta (dict)<br>projections (dict, optional) | scope=replay; effects=shader_replacement, replay_position, artifact_write; evidence=intervention | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.extract_binary` | 提取 shader 的二进制 blob（DXIL/DXBC/SPIR-V 等），并保存为文件。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`output_path`: string (optional)<br>`container`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional)<br>`as_base64`: boolean (optional) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_bindpoint_mapping` | 获取 shader 资源绑定点映射（register/space/binding -> semantic meaning）。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional) | ok (bool)<br>data (dict): {mapping}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_constant_block_layout` | 读取常量块布局（variables、offset、type、array、matrix layout）。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`block_name_or_index`: ['string', 'integer'] (required)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional) | ok (bool)<br>data (dict): {layout}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_constant_buffer_contents` | 读取指定 stage/slot 的 Constant Buffer 并按反射结构解码变量值（推荐用于专家级核对）。 | `session_id`: string (required)<br>`stage`: string (required)<br>`slot`: integer (required; min=0)<br>`array_index`: integer (optional; min=0)<br>`max_bytes`: integer (optional)<br>`flatten`: boolean (optional)<br>`event_id`: integer (optional; min=0)<br>`shader_id`: string (optional) | ok (bool)<br>data (dict): {cbuffer}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_disassembly` | 获取指定 shader 的反汇编/IR；event-bound shader 解析与 `rd.pipeline.get_shader`、`rd.export.shader_bundle`、`rd.shader.get_reflection`、`rd.shader.edit_and_replace` 使用同一套绑定逻辑。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`target`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional) | ok (bool)<br>data (dict): {disassembly, target, source_encoding, is_raw_spirv_asm, source_hash, shader_id, resolved_event_id, edit_plan}<br>`edit_plan` describes shader_format, input_kind, can_edit_text, can_build, can_replace, requires_toolchain, build_input_kind, allowed_edit_inputs, allowed_ops, recommended_next_tool, and blocked_reason. DXIL/DXBC disassembly is read-only unless edit_plan reports a buildable source path.<br>artifacts (list)<br>error (dict\|null): 绑定失败显式返回 `shader_binding_lookup_failed` / `shader_stage_mismatch`；当请求 target 不存在或源码不可取时，显式返回 `shader_disassembly_unavailable` 并带 `failure_stage` / `failure_reason`<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_messages` | 获取 RenderDoc 在 shader 编译/替换/调试过程产生的 messages。 | `session_id`: string (required)<br>`severity_min`: string (optional)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_reflection` | 获取指定 shader 的反射信息；支持 `event_id + stage` 与 `event_id + stage + shader_id` 两种 event-bound 绑定方式，显式 `shader_id` 会优先参与身份校验。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`stage`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`include_bindings`: boolean (optional)<br>`include_constant_blocks`: boolean (optional)<br>`context_id`: string (optional) | ok (bool)<br>data (dict): {reflection, shader_id, resolved_event_id}<br>artifacts (list)<br>error (dict\|null): 绑定失败显式返回 `shader_binding_lookup_failed` / `shader_stage_mismatch` / `shader_reflection_unavailable`，不再把所有失败折叠成 `shader_not_bound`<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.get_source` | 获取 shader 的源码/调试信息（若捕获包含 debug info，例如 PDB/DWARF/source embedding）。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`prefer_original`: boolean (optional)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional) | ok (bool)<br>data (dict): {source, files, source_available, fallback_tool?, fallback_args?, edit_plan, failure_stage?, failure_reason?, shader_id, stage, resolved_event_id}; when original debug source is unavailable, returns ok=true with source_available=false and a format-aware fallback. SPIR-V may recommend `rd.shader.get_disassembly` with target='SPIR-V ASM' and source_encoding='spirvasm'; DXIL/DXBC recommends read-only disassembly and/or `rd.shader.extract_binary` instead of treating disassembly text as replacement source.<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.list_entry_points` | 列出 shader 容器中的 entry points（适用于 DXIL library/多 entry SPIR-V）。 | `session_id`: string (required)<br>`shader_id`: string (optional)<br>`event_id`: integer (optional; min=0)<br>`stage`: string (optional) | ok (bool)<br>data (dict): {entry_points}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.list_replacements` | 列出当前 session 中已生效的 shader 替换。 | `session_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {replacements}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.shader.revert_replacement` | 撤销 shader 替换，恢复捕获中的原始 shader。 | `session_id`: string (required)<br>`replacement_id`: string (required)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): tool-specific payload<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=shader_replacement, replay_position; evidence=rollback | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.6，纹理数据访问与分析 (Texture Data Access & Analysis)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.texture.compute_stats` | 用于计算纹理的统计量，包括均值 (mean)、标准差 (stddev)、NaN/Inf 计数以及 0/1 占比等，以便进行快速的健全性检查 (sanity check)。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`mip`: integer (optional; min=0)<br>`slice`: integer (optional; min=0)<br>`sample`: integer (optional; min=0)<br>`stride`: integer (optional)<br>`event_id`: integer (optional; min=0) | data: {stats: {min,max,mean,nan_count,inf_count,channels}, resolved_event_id,texture_id,target_metadata}. Channel min/max exclude non-finite samples; all-nonfinite channels have null min/max. Unsupported packed/compressed formats fail explicitly. | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.diff` | 用于对比两张纹理 (要求尺寸相同且格式可转换)，并输出差异图以及各项指标，如峰值信噪比 (PSNR)、最大绝对误差 (max error) 和均方误差 (mean squared error)。 | `session_id`: string (required)<br>`tex_a`: object (required)<br>`tex_b`: object (required)<br>`metric`: array[string] (optional)<br>`event_id`: integer (optional; min=0) | data: {diff: {mse,max_abs,psnr,identical,psnr_peak,psnr_status},event_a,event_b}. Identical samples return psnr=null and psnr_status=infinite. Mismatched shapes or non-finite samples fail. No files are created. | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.get_data` | 用于读取纹理子资源的数值 readback 容器。默认返回 `.npz` artifact，供后续统计、像素分析或离线数值处理；它不是图片导出接口。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`event_id`: integer (optional; min=1)<br>`subresource`: object (optional)<br>`output_path`: string (optional)<br>`as_base64`: boolean (optional)<br>`context_id`: string (optional)<br>`state`: string (optional; enum=current,capture_initial; default="current")<br>`representation`: string (optional; enum=numeric,raw; default="numeric") | data: resource identity, byte_size, state, resolved_event_id, restored_event_id, replay_state_restored=true, initial_provenance; base64 when requested (1 MiB limit, no automatic artifact), otherwise explicit output/artifact. Initial provenance unavailable, failed/incomplete read and restoration failure are distinct errors. Restoration failure requires closing and reopening the session. | scope=replay; effects=replay_position_temporary, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.get_histogram` | 用于计算纹理子资源的直方图。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`mip`: integer (optional; min=0)<br>`slice`: integer (optional; min=0)<br>`channels`: string (optional)<br>`bins`: integer (optional)<br>`range`: object (optional)<br>`event_id`: integer (optional; min=0)<br>`sample`: integer (optional; min=0) | ok (bool)<br>data (dict): {histogram, bin_edges}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.get_pixel_history` | 用于获取当前活动事件 (active event) 下某个像素的历史信息，即哪些绘制调用 (drawcall) 或调度 (dispatch) 影响了该像素。 | `session_id`: string (required)<br>`texture_id`: string (optional)<br>`x`: integer (required; min=0)<br>`y`: integer (required; min=0)<br>`mip`: integer (optional; min=0)<br>`slice`: integer (optional; min=0)<br>`sample`: integer (optional; min=0)<br>`include_shaders`: boolean (optional)<br>`target`: object (optional)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {history}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.get_pixel_value` | 用于读取纹理在指定像素坐标处的值。该值将自动按照纹理格式解码为 float4 或 int4。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`x`: integer (required; min=0)<br>`y`: integer (required; min=0)<br>`mip`: integer (optional; min=0)<br>`slice`: integer (optional; min=0)<br>`sample`: integer (optional; min=0)<br>`as_type`: string (optional)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {value}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.get_region_values` | 用于读取纹理中一个矩形区域内的像素值，常用于局部统计分析、查找 NaN 或 Inf 值。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`rect`: object (required)<br>`mip`: integer (optional; min=0)<br>`slice`: integer (optional; min=0)<br>`sample`: integer (optional; min=0)<br>`stride`: integer (optional)<br>`as_type`: string (optional)<br>`event_id`: integer (optional; min=0) | ok (bool)<br>data (dict): {values}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.texture.render_overlay` | 用于将叠加层 (overlay) 渲染到纹理上并导出，例如绘制调用高亮、线框、视口/裁剪 (viewport/scissor) 或过度绘制 (quad overdraw) 等。 | `session_id`: string (required)<br>`texture_id`: string (required)<br>`overlay`: string (required)<br>`event_id`: integer (optional; min=0)<br>`output_path`: string (optional)<br>`image_format`: string (optional)<br>`channels`: string (optional)<br>`file_format`: string (optional)<br>`flip_y`: boolean (optional)<br>`include_alpha`: boolean (optional) | ok (bool)<br>data (dict): {image_path}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=replay; effects=replay_position, artifact_write; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.16，通用辅助工具 (Utilities, Optional)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.util.cleanup_artifacts` | 根据指定条件（如会话、前缀、时间或大小阈值）清理 artifacts。 | `session_id`: string (optional)<br>`older_than_ms`: integer (optional)<br>`prefix`: string (optional)<br>`max_total_bytes`: integer (optional) | ok (bool)<br>data (dict): {deleted, freed_bytes}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=artifact_delete; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |
| `rd.util.diff_images` | 对比两张图片（支持 PNG、JPEG、EXR 格式），输出差异图及相关指标，如 PSNR、SSIM 和最大绝对误差。 | `image_a_path`: string (required)<br>`image_b_path`: string (required)<br>`metrics`: array[string] (optional)<br>`output_path`: string (optional) | ok (bool)<br>data (dict): {metrics, diff_image_path}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=artifact_write; evidence=none | - |
| `rd.util.list_artifacts` | 列出当前会话生成的所有 artifacts，便于后续清理或归档。 | `session_id`: string (optional)<br>`prefix`: string (optional) | ok (bool)<br>data (dict): {artifacts}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict)<br>projections (dict, 可选) | scope=global; effects=none; evidence=none | session_id; via rd.capture.open_file, rd.capture.open_replay |

## 3.18，VFS 导航工具 (VFS Navigation Tools)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.vfs.cat` | 读取 read-only VFS 节点的 JSON 表示，用于路径式浏览与导航；需要精确调试、导出或状态切换时仍应回到 canonical `rd.*` tools。 | `path`: string (required)<br>`session_id`: string (optional) | ok (bool)<br>data (dict): {path, node}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict) | scope=global; effects=replay_position, artifact_write; evidence=none | - |
| `rd.vfs.ls` | 以 JSON-first 方式列出 read-only VFS 节点，用于浏览 draws/passes/resources/pipeline/context/artifacts 结构；它是导航辅助层，不是第二套调试真相。 | `path`: string (optional)<br>`session_id`: string (optional)<br>`projection`: object (optional) | ok (bool)<br>data (dict): {path, node, entries}<br>projections.tabular (dict, 可选): {format_version, columns, rows, row_count, tsv_text?} as a tabular summary for scan/readability only, not semantic ranking<br>artifacts (list)<br>error (dict\|null)<br>meta (dict) | scope=global; effects=replay_position, artifact_write; evidence=none | - |
| `rd.vfs.resolve` | 解析 VFS 路径到对应节点元数据，用于判断 path 是否存在、是否需要 session 以及当前可用的 browse-only 视图，不代替 canonical debug/export/state APIs。 | `path`: string (required)<br>`session_id`: string (optional) | ok (bool)<br>data (dict): {path, node}<br>artifacts (list)<br>error (dict\|null)<br>meta (dict) | scope=global; effects=replay_position, artifact_write; evidence=none | - |
| `rd.vfs.tree` | 按 VFS 路径返回树形 read-only 视图，适合初步浏览 session 结构或向人展示层级视图；它是 browse-only 辅助层，不代替 canonical inspection tools。 | `path`: string (optional)<br>`session_id`: string (optional)<br>`depth`: integer (optional)<br>`max_nodes`: integer (optional) | ok (bool)<br>data (dict): {path, tree, max_nodes, emitted_nodes, truncated, truncation_reason}; broad /draws tree nodes defer per-event details with detail_deferred=true and recommended_next_tool='rd.event.get_action_details' instead of embedding full action subtrees. Broad resource domains are bounded and truncated with truncation_reason='max_nodes_exceeded'. Use rd.vfs.ls or targeted rd.vfs.cat for large /resources, /textures, and /buffers exploration.<br>artifacts (list)<br>error (dict\|null)<br>meta (dict) | scope=global; effects=replay_position, artifact_write; evidence=none | - |

## 3.10，性能计数器与测量 (Performance Counters)

| Tool | Summary | Input | Result | Scope and effects | Prerequisites |
| --- | --- | --- | --- | --- | --- |
| `rd.perf.get_frame_timing` | Measure the full single-queue replay with native GPU timestamps, restoring the original event. | `session_id`: string (required)<br>`samples`: integer (optional; default=3; min=1; max=16)<br>`warmup`: integer (optional; default=1; min=0; max=4) | data: frame_timing method, unit, range, scope, sampling conditions and individual positive samples; restored_event_id and replay_state_restored. Excludes initial-content restoration, includes replay scheduling gaps, never original application time. Unsupported backend, incomplete queues and failed timestamps are errors, not zero samples. | scope=replay; effects=replay_position_temporary; evidence=measurement | session_id; via rd.capture.open_replay |
